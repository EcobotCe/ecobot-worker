name = "ecobot-api"
main = "worker.js"
compatibility_date = "2026-07-01"

# O KV não é mais usado — a lista de bases agora é fixa dentro de worker.js
# (constante BASES_FIXAS). Não precisa de nenhum binding aqui.
